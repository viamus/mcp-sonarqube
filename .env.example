# SonarQube Connection
# Your SonarQube instance base URL
SONARQUBE_BASE_URL=https://your-sonarqube-instance.com

# SonarQube user token (generate at: Your SonarQube > My Account > Security > Tokens)
SONARQUBE_TOKEN=your-token-here

# Optional SonarCloud organization key
SONARQUBE_ORGANIZATION=

# Server Security (optional)
# Set to true to require API key for all requests (except /health)
SERVER_REQUIRE_API_KEY=false

# API key that clients must provide in X-Api-Key header (required if SERVER_REQUIRE_API_KEY=true)
SERVER_API_KEY=
